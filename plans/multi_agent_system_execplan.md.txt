# ExecPlan: Build a 7-agent research/application system with OpenAI models

## Goal
Build a manager-led multi-agent system for research planning, literature mapping, project design, supervisor targeting, outreach drafting, and internal review.

## Why
The user wants a practical AI system that coordinates up to 7 specialized agents with strong custom prompts, shared state, and reliable orchestration. The system should be useful for PhD/research workflows and later extensible to other complex planning tasks.

## Scope
Implement:
- 1 manager agent:
  - ChiefOfStaff
- 6 specialist agents:
  - TopicStrategist
  - LiteratureCartographer
  - ProjectArchitect
  - SupervisorMapper
  - NarrativeWriter
  - SkepticalReviewer

Use OpenAI models and a modular architecture so prompts, schemas, tools, and orchestration are separated cleanly.

## Non-goals
- Do not build a fully autonomous unsupervised internet crawler.
- Do not implement every future workflow now.
- Do not over-optimize UI before the core orchestration works.

## Architecture
- `src/agents/`
  - one file per agent
- `src/prompts/`
  - one prompt file per agent
- `src/schemas/`
  - pydantic/json schemas for each agent output
- `src/orchestrator/`
  - manager routing logic
  - shared state
  - execution graph
- `src/tools/`
  - web search wrapper
  - file/document wrapper
- `tests/`
  - orchestration tests
  - schema validation tests
  - smoke tests for each agent

## Required behavior
- ChiefOfStaff receives the user goal and decides which agents to call.
- Each specialist returns structured JSON.
- SkepticalReviewer reviews important outputs before they are accepted.
- Shared state persists:
  - goal_profile
  - topic_pool
  - reading_board
  - project_board
  - target_supervisors
  - drafts
  - review_log
  - timeline

## Milestones

### Milestone 1: Skeleton
- Create project structure.
- Add config, environment loading, and model settings.
- Add base agent abstraction.
- Add shared state schema.
- Add prompt loader.

### Milestone 2: Core agents
- Implement ChiefOfStaff, TopicStrategist, SkepticalReviewer.
- Wire simple routing.
- Add JSON schema validation.

### Milestone 3: Research agents
- Implement LiteratureCartographer and ProjectArchitect.
- Add benchmark map + reading ladder support.
- Add project design schema.

### Milestone 4: Targeting and writing
- Implement SupervisorMapper and NarrativeWriter.
- Add output formats for ranked supervisors and outreach drafts.

### Milestone 5: Full orchestration
- Add review gates.
- Add state updates after each completed stage.
- Add logging/tracing hooks.

### Milestone 6: Validation
- Add tests for:
  - routing decisions
  - schema compliance
  - state transitions
  - review rejection/revision loops

## Constraints
- Keep prompts externalized in files.
- Keep outputs machine-readable.
- Keep agent responsibilities narrow and explicit.
- Prefer a manager-led design over free-form swarm behavior.
- Make it easy to swap models later.

## Done when
- A user can submit a high-level goal.
- The system routes the task through the right agents.
- The system returns:
  - a narrowed topic
  - a literature map
  - a first project concept
  - supervisor targets
  - draft outreach text
  - reviewer critique
- Tests pass for the main orchestration path.

## Decision log
- Manager-led orchestration chosen over peer swarm for control and auditability.
- Prompts stored as files for easier iteration.
- JSON outputs required to support chaining and validation.

## Open questions
- Whether to use Responses API only or Agents SDK + Responses API.
- Whether tool access is global or per-agent.
## Progress update

### Completed
- ✅ Milestone 1 (Skeleton)
  - Project structure created under `src/researcher_multi_agent/` with `agents/`, `orchestrator/`, `schemas/`, `utils/`, and package entrypoints.
  - Config and environment-driven model settings added.
  - Base agent abstraction added (`BaseAgent`, `DeterministicAgent`).
  - Shared state schema implemented (`GoalProfile`, canonical `SharedState`).
  - Prompt loader implemented; prompts remain externalized in files.

- ✅ Milestone 2 (Core agents)
  - Implemented `ChiefOfStaff`, `TopicStrategist`, and `SkepticalReviewer`.
  - Manager-led routing implemented with priority ordering.
  - JSON-style schema validation implemented with explicit field/literal checks and typed output objects.
  - End-to-end orchestration path implemented for first three agents (Chief → TopicStrategist → SkepticalReviewer).
  - Tests added for schema validation, routing order, orchestration path, and unhandled delegation logging.

### Still missing (outside Milestones 1-2)
- ✅ Milestone 3: `LiteratureCartographer`, `ProjectArchitect`, related schemas, and orchestrator/state updates implemented.
- ✅ Milestone 4: `SupervisorMapper`, `NarrativeWriter`, and output formats implemented.
- ✅ Milestone 5: full review gates, richer state transitions, tracing hooks implemented.
- ⏳ Milestone 6: broader validation suite (state transitions, revision loops, additional smoke/integration coverage).


### Latest update (Milestone 3)
- Added deterministic specialist agents:
  - `LiteratureCartographer`
  - `ProjectArchitect`
- Added structured output schemas for literature mapping and project design with explicit nested validation.
- Integrated both agents into the existing manager-led orchestrator via the specialist router, preserving architecture.
- Added tests covering:
  - literature-map schema validation
  - project-design schema validation
  - routing and state updates for Milestone 3 agent execution
- No blockers encountered for Milestone 3 implementation.

- Milestone 3 delegates are routed through the existing specialist-router map to keep extension points stable.


### Follow-up adjustments after Milestone 3 review
- Added dependency-aware orchestration guards:
  - `LiteratureCartographer` now requires `topic_pool`.
  - `ProjectArchitect` now requires `reading_board`.
- Added timeline events for successful specialist executions (`delegation_executed`) to make routing/state transitions auditable in tests.
- Expanded tests to cover dependency-skip behavior and stronger schema rejection cases for nested fields.
- Blockers: none.


### Follow-up adjustments after second Milestone 3 review
- Refined `ChiefOfStaff` delegation rationale text for `LiteratureCartographer` to avoid implying dependencies are already satisfied at planning time.
- Added explicit orchestration test for `LiteratureCartographer` dependency skip when `topic_pool` is missing.
- Re-ran full test suite after fixes.
- Blockers: none.


### Latest update (Milestone 4)
- Added deterministic specialist agents:
  - `SupervisorMapper`
  - `NarrativeWriter`
- Added structured output schemas for:
  - ranked supervisor targets and segmentation
  - outreach narrative drafts with customization slots
- Integrated Milestone 4 agents into the existing manager-led orchestrator via delegation routing with dependency guards:
  - `SupervisorMapper` requires `project_board`
  - `NarrativeWriter` requires `project_board` and `target_supervisors`
- Updated `ChiefOfStaff` delegations/plan to include supervisor targeting and outreach drafting in the active execution chain.
- Added tests for:
  - supervisor ranking output ordering
  - outreach draft generation
  - full Milestone 4 orchestration chain producing supervisor targets and drafts
  - schema validation/rejection for new output types
- Blockers: none.


### Follow-up adjustments after Milestone 4 review
- Strengthened `SupervisorMapperOutput` validation:
  - priorities must be sorted ascending and unique
  - segmentation names must reference known targets
- Strengthened `NarrativeWriterOutput` validation:
  - draft text must be non-empty (non-whitespace)
- Tightened orchestration dependency guards:
  - `NarrativeWriter` now also requires `project_board` in addition to `target_supervisors`
- Expanded tests to cover:
  - invalid supervisor priority ordering
  - unknown names in segmentation
  - blank outreach draft rejection
  - narrative-writer dependency skip when project context is missing
- Blockers: none.


### Follow-up adjustments after second Milestone 4 review
- Added schema test coverage for duplicate supervisor priorities to mirror uniqueness enforcement.
- Synced Milestone 4 progress notes so dependency requirements for `NarrativeWriter` are consistent across sections.
- Re-ran full test suite after updates.
- Blockers: none.


### Latest update (Milestone 5)
- Added stage-level review gates using `SkepticalReviewer` after each successfully executed specialist stage.
- Added review-gate control flow:
  - pipeline continues on `PASS`
  - pipeline blocks subsequent delegations on non-pass verdicts with explicit timeline events.
- Added persistent shared-state snapshots (`state_snapshots`) after initialization, stage executions/skips, blocked stages, and finalization.
- Added logging/tracing hooks in orchestrator:
  - standard logging events
  - optional `trace_hook` callback for programmatic tracing in tests/integrations.
- Added tests for:
  - milestone 5 end-to-end path with per-stage + final reviews
  - review-gate blocking behavior on rejection
  - tracing hook emission
- Blockers: none.


### Follow-up adjustments after Milestone 5 review
- Improved `SkepticalReviewer` deterministic behavior to evaluate stage-specific review gates against the corresponding shared-state artifact, rather than only checking topic presence.
- Applied `ChiefOfStaff.state_update.goal_profile` back into shared state so manager updates are persisted in orchestration state.
- Expanded orchestration tests to validate:
  - stage-level review verdicts are `PASS` in the full happy-path chain
  - chief goal-profile state updates are persisted.
- Re-ran full test suite after updates.
- Blockers: none.
