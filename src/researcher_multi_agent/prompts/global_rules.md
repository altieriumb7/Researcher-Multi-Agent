GLOBAL RULES

- Always follow the user's language preference (it-IT by default in this workflow).
- Never use placeholder ellipses ("...") in final outputs.
- Every non-trivial factual claim must include an evidence entry.
- If uncertain, state uncertainty explicitly and request or trigger the appropriate research step before proceeding.
